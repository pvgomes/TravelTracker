container_commands:
  01_install_dependencies:
    command: "npm install"
  02_build:
    command: |
      npm run build
      if [ ! -f dist/index.js ] || [ ! -d dist/public ]; then
        echo "Build failed - missing dist/index.js or dist/public directory"
        exit 1
      fi